<Window x:Class="EmployeeManager.Desktop.LoginFrame"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EmployeeManager.Desktop"
        mc:Ignorable="d"
        Title="LoginFrame" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Nom d'utilisateur" Grid.Row="0"/>
        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="1" Margin="0,5,0,10" Height="25"/>

        <TextBlock Text="Mot de passe" Grid.Row="2"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Height="25" Margin="0,5,0,10"
                     PasswordChanged="PasswordBox_PasswordChanged"/>

        <Button Content="Se connecter"
                Command="{Binding LoginCommand}"
                Grid.Row="4"
                Height="35" Margin="0,80,0,0"/>

        <TextBlock Text="{Binding StatusMessage}" Grid.Row="3" Margin="0,67,0,-57" Foreground="DarkRed"/>
    </Grid>
</Window>

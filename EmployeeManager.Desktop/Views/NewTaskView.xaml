<Window x:Class="EmployeeManager.Desktop.Views.NewTaskView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Nouvelle tâche" Height="630" Width="687">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>

        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Titre" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" Text="{Binding Task.Title, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2" Text="Description" Margin="0,8,0,4"/>
        <TextBox Grid.Row="3" Text="{Binding Task.Description, UpdateSourceTrigger=PropertyChanged}" 
                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Height="100"/>

        <TextBlock Grid.Row="4" Text="State" Margin="0,8,0,4"/>
        <ComboBox Grid.Row="5"
                  SelectedItem="{Binding Task.State, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  HorizontalAlignment="Left" Width="150">
            <sys:String>To_do</sys:String>
            <sys:String>Doing</sys:String>
            <sys:String>Done</sys:String>
        </ComboBox>

        <TextBlock Grid.Row="6" Text="Effort" Margin="0,8,0,4"/>
        <TextBox Grid.Row="7"  Text="{Binding Task.effortEstimation, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="8" Text="Spent Time" Margin="0,8,0,4"/>
        <TextBox Grid.Row="9" Text="{Binding Task.progress, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="11" Text="Priority" Margin="0,8,0,4"/>
        <TextBox Grid.Row="12" Text="{Binding Task.priority, UpdateSourceTrigger=PropertyChanged}" />

        <StackPanel Grid.Row="13" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Annuler" Width="100" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="💾 Enregistrer" Width="120" Command="{Binding SaveCommand}"
                    Background="#27AE60" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
